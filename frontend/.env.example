# Set this to the URL of your Laravel API to use real HTTP requests.
# If left empty (or this file is not copied to .env), the app runs in
# fully-offline demo mode against an in-memory mock backend.
#
# Example for local development:
#   REACT_APP_API_URL=http://localhost:8000/api
REACT_APP_API_URL=
